const CatalogPage = () => {
    return (
        <div>Основная страница

            {/* TODO вывод карточек товаров после загрузки с сервера <ul>
                {USERS.map(user => (
                    <li key={user.id}>
                        <Link to={`/users/${user.id}`}>
                            {user.name}
                        </Link>
                    </li>
                ))}
            </ul> */}
        </div>
    )
};

export default CatalogPage;
